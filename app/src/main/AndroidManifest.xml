<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pe.work.karique.repediatrics">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MedicalAppointmentStatesActivity"></activity>
        <activity android:name=".activities.TicketStatesActivity" />
        <activity android:name=".activities.SendResetPasswordEmailActivity" />
        <activity android:name=".activities.DoctorDetailsActivity" />
        <activity android:name=".activities.AdminParentSummaryActivity" />
        <activity android:name=".activities.AdminMainActivity" />
        <activity android:name=".activities.ConfirmAppointmentActivity" />
        <activity android:name=".activities.ChooseAppointmentActivity" />
        <activity android:name=".activities.ChooseSpecialistForMedicalAppointmentActivity" />
        <activity android:name=".activities.ChooseTicketForMedicalAppointmentActivity" />
        <activity android:name=".activities.AddTicketAtachActivity" />
        <activity android:name=".activities.AddTicketObservationActivity" />
        <activity android:name=".activities.AddTicketActivity" />
        <activity android:name=".activities.ResolveQuestionaryActivity" />
        <activity android:name=".activities.AddQuestionaryActivity" />
        <activity android:name=".activities.AddPatientActivity" />
        <activity android:name=".activities.PatientDetailsActivity" />
        <activity android:name=".activities.SpecialistMainActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.ChooseRegisterTypeActivity" />
        <activity android:name=".activities.TicketDetailsActivity" />
        <activity android:name=".activities.TicketsActivity" />
        <activity android:name=".activities.MedicalAppointmentDetailsActivity" />
        <activity android:name=".activities.MedicalAppointmentsActivity" />
        <activity android:name=".activities.DoctorMainActivity" />
        <activity android:name=".activities.AdvicesActivity" />
        <activity
            android:name=".activities.ParentMainActivity"
            android:label="@string/title_activity_parent_main" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.InitialActionActivity" />
        <activity
            android:name=".activities.FullscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_repediat" />

        <service android:name=".service.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>